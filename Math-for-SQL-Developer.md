# SQL Developer: Core Math & Logic Problem Practices

## 1. Basic Arithmetic Operations
- Add two numbers
- Subtract two numbers
- Multiply two numbers
- Divide two numbers
- Compute modulus (remainder) of two numbers
- Increment a number by a specific value (e.g., +3)
- Decrement a number by a specific value (e.g., -2)
- Compute power (e.g., 2 raised to 4)
- Compute square root of a number
- Compute square of a number
- Compute cube of a number
- Get the next number
- Get the previous number

## 2. Number Properties & Classification
- Check if a number is positive or negative
- Check if a number is zero or not
- Check if a number is even or odd
- Check if a number is prime or not
- Check if a year is a leap year

## 3. Comparison Operations
- Compare two numbers and determine which is greater
- Compare two numbers and determine which is smaller
- Check if two numbers are equal
- Check if two numbers are not equal
- Check if a number comes before or after another number
- Compare using greater than (>), less than (<), greater than or equal (>=), less than or equal (<=)

## 4. Range & Interval Checks
- Check if a number falls within a closed interval
- Check if a number falls within an open interval
- Check if age is between two values
- Check if a time is within working hours
- Validate if a time is between two times
- Clamp a time within a range
- Check if a number is within multiple ranges

## 5. Sorting & Ordering
- Sort numbers in ascending order
- Sort numbers in descending order
- Compare numbers to determine order (before/after)
- Order numbers sequentially

## 6. Counting & Iteration
- Count from 0 to n (ascending)
- Count from n to 0 (descending)
- Count from 0 to n with a specific increment
- Count from 0 to n while skipping specific values (e.g., skip even numbers)
- Count number of characters/letters in a string
- Count with continue logic

## 7. Summation & Aggregation
- Sum numbers from 1 to 10
- Sum numbers from 1 to n (user input)
- Sum odd numbers from 1 to n
- Sum even numbers from 1 to n
- Calculate average of 3 numbers
- Calculate average of 4 subjects
- Calculate total and average
- Compute mean, median, mode
- Find minimum value
- Find maximum value
- Count distinct values

## 8. Conditional Logic & Classification

### Grade & Evaluation
- Classify student grade (pass/fail)
- Assign letter grade (A, B, C, D, F)
- Evaluate grade (Excellent, Very Good, Good, Pass, Fail)

### Age & Demographics
- Classify age group (Infant, Child, Teen, Adult, Senior)

### Temperature & Environment
- Classify temperature (Freezing, Cold, Moderate, Warm, Hot, Dangerous)

### Income & Finance
- Classify income level (Low, Medium, High)
- Classify salary tier (Low, Medium, High, Executive)
- Classify tax bracket based on income

### Health & Risk
- Classify BMI category (Underweight, Normal, Overweight, Obese)
- Classify risk level (Very Low, Low, Medium, High, Critical)

### Other Classifications
- Determine season based on month number
- Determine permission level based on user role
- Process menu selection
- Interpret status codes

## 9. User Input & Validation
- Read and validate numeric input
- Check if a number is between 1 and 100
- Handle invalid or missing values
- Identify outlier values using thresholds
- Validate numeric patterns

## 10. String Manipulation
- Reverse a string/text
- Count characters/letters in a word
- Count characters in a string

## 11. Authentication & Security Logic
- Simulate login with predefined credentials
- Track failed login attempts
- Lock account after maximum failed attempts (e.g., 5 attempts)

## 12. Date & Time Calculations

### Age & Elapsed Time
- Calculate age from birthdate (years, months, days)
- Calculate elapsed time between two dates/times
- Calculate elapsed time in seconds

### Unit Conversions (Time)
- Convert seconds to minutes, hours, days
- Convert total seconds to hours, minutes, seconds
- Convert total seconds to days, hours, minutes, seconds
- Convert days to seconds / hours / minutes
- Convert hours to seconds / minutes / days
- Convert minutes to seconds / hours

### Time Arithmetic
- Add hours/minutes/seconds to a time and handle overflow (rollover)
- Convert 24-hour time to 12-hour format with AM/PM

### Date Comparison
- Determine day of the week for a given date
- Check if a time is before another time
- Check if a time is after another time

## 13. Business & Financial Math
- Calculate balance
- Calculate percentage
- Calculate percentage of total
- Calculate percentage increase/decrease
- Calculate discount
- Calculate tax
- Calculate fee
- Calculate profit, margin, and cost
- Calculate growth rate
- Calculate weighted average
- Calculate KPI metrics
- Calculate ratios and proportions
- Calculate interest

## 14. Aggregation & Grouping (SQL Style)
- Calculate group totals (sum, count per group)
- Calculate averages within groups
- Calculate min/max within groups
- Calculate ratios inside categories

## 15. Data Cleaning & Conversion
- Round a number
- Floor a number
- Ceiling a number
- Truncate a number
- Handle missing or zero values
- Convert between units (e.g., km to m, MB to GB)
- Format large numbers

## 16. Real-World Scenarios
- Sales analytics (percentages, averages)
- Inventory quantities and reorder levels
- HR calculations (salaries, bonuses, attendance)
- Financial report calculations (totals, margins)
- Data quality scoring
- Appointment/booking time validation
- Inventory level classification (Critical, Low, Sufficient, Surplus)
- Display relative time (e.g., "1 day ago", "just now")

## 17. Advanced Logic
- Multiple condition checks (AND, OR, NOT)
- Nested condition logic
- Progressive filtering (layered decision trees)
- Default fallback handling
- Priority-based decisions
- Category selection logic
- Pattern matching basics

## 18. Loop & Iteration Logic
- Reverse loop (iterate from end to beginning)
- Loop with starting point
- Loop with ending point / termination condition
- Loop with increment value
- Loop with skip logic

## 19. Programming Structure Practices
- Define and use constants
- Define and use structs / data structures
- Create functions / procedures
- Use conditional statements (IF, ELSE IF, ELSE)
- Use loop statements
- Use jumping statements
- Perform data type conversion
- Use arithmetic operators
- Use relational operators
- Use logical operators
- Use Boolean logic (TRUE / FALSE)